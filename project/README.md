# project

An interactive CLI tool to bootstrap developer workspaces instantly.

## Installation

Install it globally via npm:

```bash
npm install -g project-cli-bootstrap
```

## Usage

Navigate to an empty directory and initialize a new template:

```bash
project init
```

---
